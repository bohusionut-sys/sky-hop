# Copy to android/keystore.properties (gitignored) and fill real values.
# storeFile is resolved relative to android/app/ unless absolute.
storeFile=skyhop-release.keystore
storePassword=REPLACE_ME
keyAlias=skyhop
keyPassword=REPLACE_ME
